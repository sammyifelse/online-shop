import navBar from './navBar.mjs'

const navIconSelector = '.ham-icon';
const closeMenuSelector = '.close';
const navListSelector = '.nav-list';

navBar(navIconSelector, closeMenuSelector, navListSelector)
